import setup from '../src/setup.js';

describe('Service successfully setup', () => {
  test('can create s3 service instance', () => {
    //
  });

  test('can enumerate bucket objects', () => {
    //
  });
});
